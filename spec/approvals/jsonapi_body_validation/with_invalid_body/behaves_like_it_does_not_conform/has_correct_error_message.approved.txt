hash fails validation for key data: { hash fails validation for key attributes: { hash fails validation for key body: { Not all definitions are valid for and: { Did not pass test for max_length } }, hash fails validation for key publish_date: { Did not pass test for milliseconds_time } }, hash fails validation for key relationships: { hash fails validation for key comments: { hash fails validation for key data: { Not all items conform with each: { Item {:id=>"1", :type=>"post"} did not conform to each: { hash fails validation for key type: { Did not pass test for enum ["comment"] } } } } } } }